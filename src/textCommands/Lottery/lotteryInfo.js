const { EmbedBuilder } = require('discord.js');


module.exports = {
     name: 'lottery',
     description: 'xem thông tin xổ số',
     aliases: ['xs'],

     /**
      * 
      * @param {import('discord.js').Client} client 
      * @param {import('discord.js').Message} message 
      * @param {*} args 
      */

     callback: async (client, message, args) => { 
          
     }
}